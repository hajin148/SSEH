function flip() {
    var elem = document.getElementById("flashCard");
    if (elem.innerHTML == "Function") elem.innerHTML = "This is the definition of a function";
    else elem.innerHTML = "Function";
}